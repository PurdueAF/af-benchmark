executor:
  backend: sequential
  # backend: futures
  # backend: dask-local
  # backend: dask-gateway
data-access:
  mode: local
  files: 
    - tests/data/nano_dimuon.root
processing:
  method: uproot
  # method: nanoevents
  columns:
    - event
    - Muon_pt
  operation: mean
  write: false
measurements:
  what:
    - timing
    - ram
  report:
    - print
