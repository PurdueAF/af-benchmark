executor:
  backend: sequential
data-access:
  mode: explicit-files
  files: 
    - tests/data/nano_dimuon.root
processor:
  parallelize_over: files
  columns:
    - event
    - Muon_pt
  operation: sum
